# sdd-scaffolding — Product Requirements Document

**Status:** Living · **Started:** 2026-09-16

## Grounding Index

Use this table instead of reading the entire PRD. Read **Problem Statement**, **Product Vision** and
**Target Users** as the universal product baseline, then read only the requirement rows matching the
work's candidate `prd:`, `arch:` and `area:` tags. The tag vocabulary is curated in
`docs/artifacts/README.md`. Add a row when a requirement section is added.

| Product surface | Requirement tags | Architecture / area tags | Read these sections |
|-----------------|------------------|--------------------------|---------------------|
| Product purpose and users | — | all areas | `Problem Statement`; `Product Vision`; `Target Users` |
| TODO | `prd:FR-1` | TODO | `FR-1: <title>` |

## Problem Statement

TODO — what is hard today, for whom, and why existing options fall short.

## Product Vision

TODO — one paragraph on what sdd-scaffolding is when it is done well.

## Target Users

TODO — the roles that use it and what each one needs from it.

## Functional Requirements

Requirements are numbered `FR-N` and never renumbered; a retired requirement is marked
**Retired <date>** and kept. Each one states the capability, the constraints and the acceptance
signal; the stories doc (config `repo.docs.stories`) carries the per-story acceptance criteria.

### FR-1: <title>

TODO — the capability, in one paragraph, followed by bullets for the constraints that bound it.

## Non-Functional Requirements

Numbered `NFR-N`, same rules as above.

### NFR-1: <title>

TODO
